services:
  caddy:
    build: ./caddy
    ports:
      - "127.0.0.1:8080:80"
    volumes:
      - ./data:/data:ro
      - ./web:/srv/web:ro
      - ./fonts/output:/srv/fonts:ro
    restart: unless-stopped
